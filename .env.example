# OpenAI API Key (obrigatório)
# Chave de autenticação para a API OpenAI usada na geração de embeddings
OPENAI_API_KEY=sk-...

# PostgreSQL — Configurações de conexão (todas obrigatórias)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tallo_rag
DB_USER=postgres
DB_PASSWORD=your_password_here

# Dialeto SQL (opcional, padrão: unknown)
# Valores aceitos: sybase, oracle, sqlserver
SQL_DIALECT=unknown